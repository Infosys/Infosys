// Minimal Vitest setup file
// Registers DOM testing helpers (jest-dom) and any global mocks needed for tests.
import '@testing-library/jest-dom';

// You can add global mocks or window polyfills here later.

export {};
