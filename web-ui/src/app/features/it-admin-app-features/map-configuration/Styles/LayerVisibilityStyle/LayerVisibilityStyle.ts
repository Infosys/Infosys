import type { SxProps, Theme } from "@mui/material";

export const layerVisibilityContainerSx: SxProps<Theme> = {
    pl: 5,
    borderRadius: "10px",
}

export const ToggleCardListSx: SxProps<Theme> = {
    display: "flex",
    flexDirection: "column",
}